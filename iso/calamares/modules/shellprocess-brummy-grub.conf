# Fundo do Brummy e splash do Plymouth no GRUB. Depois do grubcfg (que
# reescreve /etc/default/grub) e antes do bootloader (que roda grub-mkconfig).
---
dontChroot: false
timeout: 60
script:
  - "/usr/local/bin/brummy-pos-instalacao grub"
