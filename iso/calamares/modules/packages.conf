# Depois de copiar o live para o disco, tira o que só fazia sentido no live.
---
backend: pacman
# Remover não precisa de rede: sem isto, instalar offline deixaria o
# Calamares e o archiso no sistema.
skip_if_no_internet: false
update_db: false
update_system: false

operations:
  - remove:
      # @@PACOTES_LIVE@@ — o build-iso.sh troca esta linha pela lista gerada
      # de iso/profile/packages.live (gen-packages.sh --remocao).
