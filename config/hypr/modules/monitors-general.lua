-- Brummy — perfil geral: qualquer PC, qualquer monitor.
-- Usado quando --profile general ou quando o hardware não é reconhecido.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = "auto" })
